# MEA Part 058 — Governance Mechanisms (§7~§17 통합)

> **거버넌스 상태**: 거버넌스 메커니즘 설계 · 코드 변경 0 · NOT_CERTIFIED · 289차 후속(2026-07-22).
> ★`AutoRecommend`(폐루프 추천)·`Decisioning`(세그먼트)·**`Mmm`**(ROI 최적화 정본)·**`PriceOpt`**(가격 9테이블)·`RuleEngine`(임계 규칙)·`AutoCampaign`(자율 실행·정직 보류·`optimization_log`)·`AdAdapters`(액추에이터·agent_mode)·`DemandForecast`·`JourneyBuilder`/`Alerting`(054 정본)·`ClaudeAI`(053)·`SecurityAudit`(056)·`SystemMetrics`(057)·`Crypto`/`index.php` 재사용(★**8번째 결정 엔진 신설 절대 금지**=헌법 V4)·**Decision Registry/표준 계약/Rule 거버넌스 순신설**·오흡수 금지·과대주장 금지·**부재 축소 금지**·**★★마케팅 AI/dev AI KEEP_SEPARATE**·Part 042/044/046/047~049/**051~057**·EPIC 06-A 상속.
> ★**Part 054 판정 상속·재판정 금지**·**경계 고정: 054=Agent/Workflow 실행 계층, 058=Decision 계층**.

## §7 Decision Lifecycle 거버넌스
Decision Request→Context Collection→Data Analysis→Alternative Generation→Decision Evaluation→Approval→Execution→Monitoring→Optimization→Archive. 현행=**Context Collection 부분**(집계 조립 `Decisioning`:460~470·`ClaudeAI` 053:1497)·**Data Analysis**(ROAS/CPA/이익/탄력성)·**Alternative Generation 부분**(`Mmm::frontier` 채널별 최적 지출:386~391·`po_recommendations` `PriceOpt`:91·`AutoRecommend::recommend`:506)·**Approval**(`Alerting` 2인 정족수:602·`agent_mode` 054:42~50)·**Execution**(`AutoCampaign`:345~350·`AdAdapters`)·**Monitoring 부분**(실행 로그 4종)·**Optimization**(폐루프 `learnFromOutcomes`:247). ★**Decision Request·Decision Evaluation(형식)·Archive**=순신설.
★★**§7 핵심 미충족**: "모든 의사결정은 **추적 가능**해야 한다" → **도메인별 로그는 남지만 통합 추적 불가**. "오늘 이 테넌트에서 어떤 의사결정이 몇 건 났고 무엇이 승인/거부/보류됐는가"를 답할 **단일 지점이 없다**. ★해결은 **새 로그가 아니라** 기존 4종(`optimization_log` `AutoCampaign`:77·`rule_engine_log` `RuleEngine`:47·`po_repricer_history` `PriceOpt`:132·`journey_node_logs` 054)을 **DECISION_EXECUTION 뷰/참조로 통합**(★**원본 테이블 파괴 금지=무회귀**).

## §8 Decision Engine 거버넌스
Rule Evaluation·AI Recommendation·Multi-Criteria·Risk Assessment·ROI Evaluation·Scenario Comparison·Priority Ranking·Decision Execution. 현행=**Rule Evaluation**(`RuleEngine::evaluateAll`:181)·**AI Recommendation 3경로**(`AutoRecommend`:506·`Decisioning`:432·`ClaudeAI` 053:1894)·**ROI Evaluation**(`Decisioning`:466~470·`Mmm`)·**Priority Ranking**(ROAS 정렬·상위 N:486~487)·**Decision Execution**(`AutoCampaign`·`AdAdapters`)·Risk Assessment 부분(`Risk` 056:31~60). ★**Multi-Criteria Analysis(형식)·Scenario Comparison(서버 통합)**=순신설.
★★**설계 원칙(중복 차단)**: 통합 Decision Engine은 **새 결정 로직이 아니라 디스패처**다 — DECISION_OPTION 생성은 `AutoRecommend`/`Decisioning`/`Mmm`/`PriceOpt`를 **호출**해 얻는다(★새 추천·최적화 로직 신설 금지=헌법 V4). ★Multi-Criteria 도입 시 현행 암묵 복합 판단(`Decisioning`:466~478 ROAS+CPA+spend)의 **가중치·기준을 코드 상수에서 정책 객체로 승격**하되 **현행 임계값을 그대로 seed**로 이관(무회귀).

## §9 Business Rule Engine 거버넌스
Rule Definition·Versioning·Simulation·Validation·Deployment·Conflict Detection·Optimization·Analytics. 현행=**Rule Definition**(`rule_engine` metric/op/threshold/action :41~46·`saveRule`:111·`toggleRule`:137·`deleteRule`:146)·Rule 실행(:181)·**리프라이서 규칙**(`po_repricer_rules` `PriceOpt`:121)·**daypart/frequency 제어**(`RuleEngine`:54 daypart_schedule·:60~66 frequency_window/event). ★**Versioning·Simulation·Validation·Deployment·Conflict Detection·Optimization·Analytics**=순신설.
★★**§9 핵심 미충족(정직 표기)**: "모든 Rule은 **버전과 변경 이력**을 관리한다" → **미충족**. `rule_engine`은 **현재값만 보관**(UPDATE 덮어쓰기)이고 **`rule_engine_log`(:47)는 실행 로그이지 변경 이력이 아니다**(오흡수 금지). ★**Rule Versioning 도입 시 append-only 이력 테이블 신설**이 정본 경로이며, 이는 **중복이 아니라 결여 보강**이다. ★**Rule Conflict Detection**은 현행 다중 규칙 동시 발화 시 **상충 액션(예: 예산 증액 vs 캠페인 중지)을 막을 장치가 없다**는 실제 위험을 다룬다 — 설계 시 **`AdAdapters` 액추에이터 진입점에서 최종 상충 검사**(중복 게이트 신설 금지).
★★**Rule Simulation 도입 시 필수 제약**: 시뮬레이션이 **실 액추에이터를 호출하면 안 된다**(드라이런 격리). `AutoCampaign`/`AdAdapters` 실행 경로와 **명확히 분리**하고, 킬스위치·`agent_mode` 게이트를 우회하지 않을 것.

## §10 Decision Optimization 거버넌스
What-if·Predictive·Cost·Resource·Schedule·Route·KPI·ROI Optimization. 현행=★**본 Part 최고 실재분** — **ROI Optimization**(`Mmm::frontier`:349~352 **적정 총예산 T\*·PROFIT(T) 곡선·증액여력**:386~391/:437·한계이익 수식 :281 `margin_c·(β/κ)·exp(−x/κ)−1`·`x*_c=κ·ln(margin_c·β/κ)`·`T*=Σx*_c`·`profitOptSpend`:337~338)·**KPI Optimization 부분**·**Cost Optimization**(`PriceOpt` 탄력성:81·추천:91)·**What-if Analysis**(`PnLDashboard.jsx`:538~556 5레버 즉시 순이익 재계산·**클라이언트**)·**Scenario 부분**(`po_simulations`:105)·**Predictive Optimization**(`DemandForecast`). ★**Resource·Schedule·Route Optimization**=순신설(grep 0).
★★**"최적화 결과는 정량적 근거를 제공해야 한다" → 충족(실측)**: `Mmm::frontier`가 수식 기반 곡선·T\*·증액여력을 반환하고, **모델·원가가 없으면 `optimized:false`+사유를 정직 반환**(:375 frontierNoModel·:378 frontierNoCost+`needs:sku_cost`)한다. ★**이 패턴이 본 저장소의 모범**이며 신규 최적화/결정 지표도 **측정·산출 불가 시 0이나 임의값이 아니라 명시적 미산출+사유**로 반환할 것([[feedback_real_value_autoderive]]·057 D-2 규율 승계).
★**What-if 서버 승격 시 제약**: `PnLDashboard` 로직을 서버로 올릴 때 **클라이언트 즉시성(슬라이더 반응)을 잃지 말 것**(하이브리드)·**기존 프론트 계산 제거는 회귀**.

## §11 Autonomous Business 거버넌스 (★대부분 054 소관·재판정 금지)
Autonomous Workflow·Intelligent Recommendation·Event-Based Automation·Business Process Optimization·Decision Scheduling·Exception Handling·Human Approval Workflow·Continuous Improvement. 현행=**Autonomous Workflow**(`JourneyBuilder` 054 PARTIAL-strong)·**Intelligent Recommendation**(`AutoRecommend`)·**Event-Based Automation**(트리거 detector 054)·**Decision Scheduling**(cron 36·daypart `RuleEngine`:54)·**Human Approval Workflow**(`Alerting` 2인:602·:626~632)·**Continuous Improvement**(`learnFromOutcomes`:247)·**★Exception Handling 실재**(`AutoCampaign`:345~350 — auto 모드라도 **킬스위치·결제수단·딜리버리 미충족 시 활성화 보류 + 사유 반환**). ★**Business Process Optimization(형식)**=순신설.
★★**"자율 운영은 승인 정책을 준수해야 한다" → 충족**: `agent_mode` 3모드(**기본 approval fail-safe**)·auto도 **킬스위치 종속**·미충족 시 정직 보류. ★★**후퇴 금지 자산** — 어떤 Decision Platform 개편도 이 게이트를 **우회하거나 약화시키면 즉시 회귀**([[feedback_no_regression_value_unification]]). 특히 "통합 Decision Engine이 직접 실행"하는 설계는 **`agent_mode`·킬스위치·결제수단/딜리버리 게이트를 반드시 경유**해야 한다.

## §12 Decision Governance
Decision/Approval/Rule/Optimization/Exception/Risk Policy·Compliance Validation·Audit Trail. 현행=Approval Policy 부분(정족수·권한모드)·Audit Trail 부분(`SecurityAudit` 056). ★**Policy 객체 6종·Compliance Validation**=순신설.
★**정책 원문 재정의 금지**(056 D-1 승계): 헌법 V5 Safety Rule·`CHANGE_GATE`가 **규범 정본**이며 Decision Policy는 이를 **기계 집행**할 뿐 새 규범을 만들지 않는다(값 분산=회귀).

## §13 Data Security 거버넌스
Tenant Isolation·RBAC·Decision Data Encryption·Policy Protection·Immutable Audit Log·Audit Logging. 현행=**Tenant Isolation**(fail-closed 테넌트 해석 `Risk` 056:15~18·도메인 테이블 tenant 키)·**RBAC**(서버측 전역 writeGuard 056:72~75)·**Immutable Audit Log**(`SecurityAudit` 해시체인 056)·Audit Logging. ★**Decision Data Encryption**(`Crypto` 049 재사용 가능·미적용)·**Policy Protection**(정책 객체 자체 부재)=순신설.
★★"의사결정 데이터는 **승인된 사용자만 접근**할 수 있어야 한다" → **형식 ACL 부재**(현행은 테넌트+RBAC 일반 게이트). ★**테넌트 격리 절대** — 의사결정 데이터에는 **예산·가격·마진·탄력성**이 담겨 교차 노출 시 **영업 기밀 유출**([[reference_platform_growth_actas_tenant_hijack]]).
★**감사 체인 주의**(056 D-3·057 D-4 승계): DECISION_AUDIT은 **새 해시체인이 아니라 `SecurityAudit` 확장**이되, **고빈도 결정 로그를 체인에 직접 넣지 말고**(체인 붕괴) 구간 앵커링으로 무결성 보장.

## §14 Runtime 규칙 거버넌스
Context 수집·Rule Evaluation·AI Recommendation 생성·Risk Assessment·Decision 실행·Event 생성·Audit 기록. 현행=Context 수집 부분·**Rule Evaluation**(:181)·**AI Recommendation 생성**(3경로)·**Decision 실행**(`AutoCampaign`·`AdAdapters`)·Audit 기록 부분. ★**형식 Risk Assessment 삽입·Event 생성·Decision 전용 Audit**=순신설.
★**성능(§18)**: Decision Evaluation ≤500ms·Rule Execution ≤100ms·Recommendation ≤2s·Decision API ≤300ms는 **측정 장치 부재** → ★**"미달"이 아니라 "측정 기반 부재"**(057 규율 승계). 계측은 **`SystemMetrics` 확장**으로(057 D-1·별도 수집기 금지).
★**후퇴 금지 자산**: 기본 approval fail-safe·킬스위치 종속 auto·정직 보류+사유·2인 정족수·`optimized:false` 정직 반환·테넌트 fail-closed·전역 writeGuard.

## §15 API 거버넌스 (8)
Execute Decision·Evaluate Rule·Optimize Decision·Query Decision Status/Analytics/Policy·Register Decision·Query Decision Audit. 현행=**도메인별 API 다수**(`/v424/mmm/frontier`·`/v424/mmm/optimize`·`RuleEngine::runEndpoint`:166/`logs`:155·`Decisioning::recommendations`:432·`AutoRecommend::recommend`:506·PriceOpt 계열). ★**Decision 표준 API 8종**=순신설.
★★**보안(053 D-5·057 D-7 승계)**: 거버넌스·감사 API와 마찬가지로 **Decision API는 전량 인증 필수 접두**에 배치한다 — 공개 bypass 접두에 얹으면 **경영 의사결정(예산·가격·마진)이 무인증 노출**된다. `/api` 접두 변형 동시 등재 필수([[reference_api_prefix_routing]]).

## §16 Event 거버넌스 (8)
DecisionRequested·RuleEvaluated·RecommendationGenerated·DecisionApproved·DecisionExecuted·DecisionRejected·DecisionOptimized·DecisionAudited. 현행=DecisionExecuted 유사=`optimization_log`/`rule_engine_log` INSERT·DecisionApproved/Rejected 유사=`action_request` 상태 전이(054/056). ★**Event 표준 8종 전부 순신설**(★DB write·상태 전이≠이벤트 버스 오흡수 금지·Part 046/057 Observability 정합·중복 이벤트 파이프라인 신설 금지).

## §17 AI Integration 거버넌스
Intelligent Recommendation·Decision Optimization·Scenario Simulation·Risk Prediction·KPI Impact Analysis·ROI Prediction·Explainable Decision·Responsible Decision Validation. 현행=**Intelligent Recommendation**(3경로)·**Decision Optimization**(`Mmm`·`PriceOpt`)·**Risk Prediction**(`Risk` 056)·**ROI Prediction 부분**(PROFIT(T) 곡선·증액여력)·**Explainable Decision 부분**(`Decisioning` 공시:477~481·`Risk` drivers 056·`ClaudeAI` 근거 강제 053:876). ★**Scenario Simulation(통합)·KPI Impact Analysis(형식)·Responsible Decision Validation**=순신설.
★★명세 §17 말미 **"AI는 승인 정책 없이 중요 경영 의사결정을 자동 확정하거나 기업 정책을 변경하지 않는다" → 현행이 구조적으로 충족**: ⓐ자율 실행은 **`agent_mode='auto'` + 킬스위치 + 결제수단·딜리버리 게이트 통과 시에만**이고 미충족 시 **정직 보류+사유 반환**(`AutoCampaign`:345~350) ⓑ**기본값은 approval**(fail-safe·054:42~50) ⓒ**기업 정책이 문서/코드**라 AI가 변경할 대상이 없다(056 D-7) ⓓ파괴적 액션은 **제안-only+HITL**(054 D-2) ⓔ승인 필요 액션은 **2인 정족수**(`Alerting`:602). ★헌법 V5+`CHANGE_GATE`+배포 승인([[feedback_deploy_approval_mandatory]]). **★후퇴 금지 자산** — "중요 경영 의사결정"의 범위를 넓힐수록(예산 재배분·가격 변경) **승인 게이트를 더 엄격히** 적용해야 하며, 통합 Decision Engine이 이를 우회하면 **명세 §17 + 헌법 V5 동시 위반**이다.

## 판정
전 메커니즘 **설계-only·코드 0·NOT_CERTIFIED.** ★**실 의사결정 거버넌스**=**ROI 최적화 정본**(`Mmm::frontier` 한계이익 수식·T\*·PROFIT 곡선·증액여력 + **`optimized:false` 정직 반환**)+**가격 결정 스택**(`PriceOpt` 탄력성·추천·시뮬레이션·리프라이서 규칙·실행 이력)+**폐루프 학습 추천**(`AutoRecommend` 벤치마크·학습 prior·`learnFromOutcomes`)+**세그먼트 추천·우선순위**(`Decisioning`)+**규칙 평가·daypart·frequency**(`RuleEngine`)+**자율 실행 + ★정직 보류**(`AutoCampaign` 킬스위치/결제수단/딜리버리 미충족 시 사유 반환)+**승인 정책**(`agent_mode` 기본 approval·2인 정족수)+**What-if 5레버**(클라이언트)+**실행 로그 4종**+**Explainable 공시**+테넌트 fail-closed·전역 writeGuard·해시체인 감사=**후퇴 금지 자산**. ★**Enterprise Decision Registry·Canonical Entity 15종 형식 계약·통합 Decision Engine(디스패처)·Rule Versioning/Simulation/Validation/Deployment/Conflict Detection/Optimization/Analytics·DECISION_VERSION·Multi-Criteria(형식)·Scenario Comparison(통합)·Resource/Schedule/Route Optimization·Decision Policy 6종·Compliance Validation·Decision Analytics/Dashboard/Advisor·Decision Data Encryption·형식 ACL·API 8종·Event 8종·성능 계측=순신설**(부재·grep 0·부재증명 완료). ★★**본 Part의 성격**=**"결정 엔진이 없다"가 아니라 "결정 엔진이 7개인데 통합 Registry가 없다"** — Decision Platform은 **8번째 엔진이 아니라 기존 7개 위의 얇은 통합 계층(Registry+표준 계약+뷰+디스패처)**이어야 한다(헌법 V4). ★★**설계 제약**=ⓐ**추천·최적화·규칙·승인·실행 로그 이원화 금지**(전부 기존 정본 재사용·실행 로그 4종은 **원본 유지 + 뷰 통합**) ⓑ**통합 Engine도 `agent_mode`·킬스위치·결제수단/딜리버리 게이트를 반드시 경유**(우회 시 회귀) ⓒ**Rule Simulation은 실 액추에이터 호출 금지**(드라이런 격리) ⓓ**Rule Conflict Detection은 `AdAdapters` 진입점에서 최종 상충 검사**(중복 게이트 금지) ⓔ**산출 불가 시 0/임의값이 아니라 명시적 미산출+사유**(`Mmm` 패턴 승계) ⓕ**Decision API는 인증 필수 접두**(경영 의사결정 무인증 노출 방지) ⓖ**감사 체인에 고빈도 결정 로그 직접 유입 금지**(앵커링). ★오흡수 금지(**`whatif`=`PnLDashboard.jsx` 클라이언트 슬라이더≠서버 최적화 서비스**·**`autonomous`=`AIInsights.jsx`:599 마케팅 카피≠구현**·`RuleEngine` 임계값≠Business Rule Engine·**`rule_engine_log`(실행)≠Rule 변경 이력**·`Decisioning`(광고 세그먼트)≠Decision Intelligence 플랫폼·`AutoCampaign`(캠페인)≠Autonomous Business·`Mmm::frontier`(마케팅 예산)≠범용 Decision Optimization·`JourneyBuilder`(마케팅 여정·054 소관)≠비즈니스 의사결정 엔진·`Risk`(사업 리스크·056)≠Decision Risk Assessment·`po_simulations`(가격)≠통합 Scenario Simulation·`SecurityAudit`≠DECISION_AUDIT 엔티티). 헌법 Volume 4/5·데이터 헌법 V3·Part 042/044/046/047/048/049/**051~057**·EPIC 06-A 상속·**재판정 금지**·재감사 금지(287/288차 action_request 생산자·279차 auto 모드 확정분)·★★마케팅 AI(`ClaudeAI`)/dev AI(Claude Code) KEEP_SEPARATE·★승인 정책 없는 중요 경영 의사결정 자동 확정·기업 정책 변경 불가(V5+CHANGE_GATE).
